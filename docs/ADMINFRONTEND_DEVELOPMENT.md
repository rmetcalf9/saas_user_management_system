# adminfrontend development

There are two ways to run adminfrontend from the directory.

1. Complete system native on dev machine (no containers involved)
2. Run against a container with saas_user_management

## Run native on dev machine

See run dev local for instructions on running - it's the same.

## Run against container

Go into the admin frontend directory.
use ./run_against_container_backend
